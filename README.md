# @appneural/os-http

Common HTTP router, JSON response, request body parsing, health checks, CORS, and error responses.

```ts
import { Router, createOsServer, jsonResponse } from "@appneural/os-http";
```
